discard """
  description: "Make sure the `Except` parameter is allocated properly"
"""
(TypeDefs
  (Int 8)
  (ProcTy (Void))
)
(GlobalDefs)
(ProcDefs
  (ProcDef (Type 1) 0
    (List
      (Block (Params)
        (Locals (Type 0))
        (Asgn (Local 0) (IntVal 100))
        (Raise (IntVal 200)
          (Goto 1 (List (Local 0)))))
      (Except (Params (Type 0) (Type 0)) (Locals)
        (Drop (Move (Param 0)))
        (Drop (Move (Param 1)))
        (Return)))))
